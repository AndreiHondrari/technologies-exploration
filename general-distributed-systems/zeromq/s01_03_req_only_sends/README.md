# Requester only sends and replier only receives

Attempting in doing this will result in error because the requester always
sends and then expects a reply, and the replier always receives and the
sends.
